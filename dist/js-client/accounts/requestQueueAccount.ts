/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getQueueHeaderDecoder,
  getQueueHeaderEncoder,
  getRequestItemDecoder,
  getRequestItemEncoder,
  type QueueHeader,
  type QueueHeaderArgs,
  type RequestItem,
  type RequestItemArgs,
} from '../types';

export const REQUEST_QUEUE_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  253, 95, 14, 171, 107, 113, 169, 186,
]);

export function getRequestQueueAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_QUEUE_ACCOUNT_DISCRIMINATOR
  );
}

export type RequestQueueAccount = {
  discriminator: ReadonlyUint8Array;
  header: QueueHeader;
  entries: Array<RequestItem>;
};

export type RequestQueueAccountArgs = {
  header: QueueHeaderArgs;
  entries: Array<RequestItemArgs>;
};

export function getRequestQueueAccountEncoder(): Encoder<RequestQueueAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['header', getQueueHeaderEncoder()],
      ['entries', getArrayEncoder(getRequestItemEncoder(), { size: 0 })],
    ]),
    (value) => ({
      ...value,
      discriminator: REQUEST_QUEUE_ACCOUNT_DISCRIMINATOR,
    })
  );
}

export function getRequestQueueAccountDecoder(): Decoder<RequestQueueAccount> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['header', getQueueHeaderDecoder()],
    ['entries', getArrayDecoder(getRequestItemDecoder(), { size: 0 })],
  ]);
}

export function getRequestQueueAccountCodec(): Codec<
  RequestQueueAccountArgs,
  RequestQueueAccount
> {
  return combineCodec(
    getRequestQueueAccountEncoder(),
    getRequestQueueAccountDecoder()
  );
}

export function decodeRequestQueueAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<RequestQueueAccount, TAddress>;
export function decodeRequestQueueAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<RequestQueueAccount, TAddress>;
export function decodeRequestQueueAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
):
  | Account<RequestQueueAccount, TAddress>
  | MaybeAccount<RequestQueueAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRequestQueueAccountDecoder()
  );
}

export async function fetchRequestQueueAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<RequestQueueAccount, TAddress>> {
  const maybeAccount = await fetchMaybeRequestQueueAccount(
    rpc,
    address,
    config
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRequestQueueAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<RequestQueueAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRequestQueueAccount(maybeAccount);
}

export async function fetchAllRequestQueueAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<RequestQueueAccount>[]> {
  const maybeAccounts = await fetchAllMaybeRequestQueueAccount(
    rpc,
    addresses,
    config
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRequestQueueAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<RequestQueueAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeRequestQueueAccount(maybeAccount)
  );
}
