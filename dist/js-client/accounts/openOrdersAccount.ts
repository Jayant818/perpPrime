/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getOrderDecoder,
  getOrderEncoder,
  type Order,
  type OrderArgs,
} from '../types';

export const OPEN_ORDERS_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  255, 194, 78, 123, 16, 105, 208, 165,
]);

export function getOpenOrdersAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    OPEN_ORDERS_ACCOUNT_DISCRIMINATOR
  );
}

export type OpenOrdersAccount = {
  discriminator: ReadonlyUint8Array;
  owner: Address;
  market: Address;
  bump: number;
  padding: ReadonlyUint8Array;
  orders: Array<Order>;
  clientOrderIds: Array<bigint>;
};

export type OpenOrdersAccountArgs = {
  owner: Address;
  market: Address;
  bump: number;
  padding: ReadonlyUint8Array;
  orders: Array<OrderArgs>;
  clientOrderIds: Array<number | bigint>;
};

export function getOpenOrdersAccountEncoder(): Encoder<OpenOrdersAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['owner', getAddressEncoder()],
      ['market', getAddressEncoder()],
      ['bump', getU8Encoder()],
      ['padding', fixEncoderSize(getBytesEncoder(), 7)],
      ['orders', getArrayEncoder(getOrderEncoder(), { size: 64 })],
      ['clientOrderIds', getArrayEncoder(getU64Encoder(), { size: 64 })],
    ]),
    (value) => ({ ...value, discriminator: OPEN_ORDERS_ACCOUNT_DISCRIMINATOR })
  );
}

export function getOpenOrdersAccountDecoder(): Decoder<OpenOrdersAccount> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['owner', getAddressDecoder()],
    ['market', getAddressDecoder()],
    ['bump', getU8Decoder()],
    ['padding', fixDecoderSize(getBytesDecoder(), 7)],
    ['orders', getArrayDecoder(getOrderDecoder(), { size: 64 })],
    ['clientOrderIds', getArrayDecoder(getU64Decoder(), { size: 64 })],
  ]);
}

export function getOpenOrdersAccountCodec(): Codec<
  OpenOrdersAccountArgs,
  OpenOrdersAccount
> {
  return combineCodec(
    getOpenOrdersAccountEncoder(),
    getOpenOrdersAccountDecoder()
  );
}

export function decodeOpenOrdersAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<OpenOrdersAccount, TAddress>;
export function decodeOpenOrdersAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<OpenOrdersAccount, TAddress>;
export function decodeOpenOrdersAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
):
  | Account<OpenOrdersAccount, TAddress>
  | MaybeAccount<OpenOrdersAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getOpenOrdersAccountDecoder()
  );
}

export async function fetchOpenOrdersAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<OpenOrdersAccount, TAddress>> {
  const maybeAccount = await fetchMaybeOpenOrdersAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeOpenOrdersAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<OpenOrdersAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeOpenOrdersAccount(maybeAccount);
}

export async function fetchAllOpenOrdersAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<OpenOrdersAccount>[]> {
  const maybeAccounts = await fetchAllMaybeOpenOrdersAccount(
    rpc,
    addresses,
    config
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeOpenOrdersAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<OpenOrdersAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeOpenOrdersAccount(maybeAccount)
  );
}
