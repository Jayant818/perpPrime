/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PERP_PRIME_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const LIQUIDATE_POSITION_DISCRIMINATOR = new Uint8Array([
  187, 74, 229, 149, 102, 81, 221, 68,
]);

export function getLiquidatePositionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LIQUIDATE_POSITION_DISCRIMINATOR
  );
}

export type LiquidatePositionInstruction<
  TProgram extends string = typeof PERP_PRIME_PROGRAM_ADDRESS,
  TAccountLiquidator extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountUserPosition extends string | AccountMeta<string> = string,
  TAccountOpenOrderAccount extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountRequestQueue extends string | AccountMeta<string> = string,
  TAccountBaseMint extends string | AccountMeta<string> = string,
  TAccountQuoteMint extends string | AccountMeta<string> = string,
  TAccountPriceFeed extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountLiquidator extends string
        ? WritableSignerAccount<TAccountLiquidator> &
            AccountSignerMeta<TAccountLiquidator>
        : TAccountLiquidator,
      TAccountUser extends string
        ? ReadonlyAccount<TAccountUser>
        : TAccountUser,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountUserPosition extends string
        ? WritableAccount<TAccountUserPosition>
        : TAccountUserPosition,
      TAccountOpenOrderAccount extends string
        ? WritableAccount<TAccountOpenOrderAccount>
        : TAccountOpenOrderAccount,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      TAccountRequestQueue extends string
        ? WritableAccount<TAccountRequestQueue>
        : TAccountRequestQueue,
      TAccountBaseMint extends string
        ? ReadonlyAccount<TAccountBaseMint>
        : TAccountBaseMint,
      TAccountQuoteMint extends string
        ? ReadonlyAccount<TAccountQuoteMint>
        : TAccountQuoteMint,
      TAccountPriceFeed extends string
        ? ReadonlyAccount<TAccountPriceFeed>
        : TAccountPriceFeed,
      ...TRemainingAccounts,
    ]
  >;

export type LiquidatePositionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type LiquidatePositionInstructionDataArgs = {};

export function getLiquidatePositionInstructionDataEncoder(): FixedSizeEncoder<LiquidatePositionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: LIQUIDATE_POSITION_DISCRIMINATOR })
  );
}

export function getLiquidatePositionInstructionDataDecoder(): FixedSizeDecoder<LiquidatePositionInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getLiquidatePositionInstructionDataCodec(): FixedSizeCodec<
  LiquidatePositionInstructionDataArgs,
  LiquidatePositionInstructionData
> {
  return combineCodec(
    getLiquidatePositionInstructionDataEncoder(),
    getLiquidatePositionInstructionDataDecoder()
  );
}

export type LiquidatePositionAsyncInput<
  TAccountLiquidator extends string = string,
  TAccountUser extends string = string,
  TAccountUserAccount extends string = string,
  TAccountUserPosition extends string = string,
  TAccountOpenOrderAccount extends string = string,
  TAccountMarket extends string = string,
  TAccountRequestQueue extends string = string,
  TAccountBaseMint extends string = string,
  TAccountQuoteMint extends string = string,
  TAccountPriceFeed extends string = string,
> = {
  liquidator: TransactionSigner<TAccountLiquidator>;
  user: Address<TAccountUser>;
  userAccount?: Address<TAccountUserAccount>;
  userPosition?: Address<TAccountUserPosition>;
  openOrderAccount?: Address<TAccountOpenOrderAccount>;
  market?: Address<TAccountMarket>;
  requestQueue?: Address<TAccountRequestQueue>;
  baseMint: Address<TAccountBaseMint>;
  quoteMint: Address<TAccountQuoteMint>;
  priceFeed: Address<TAccountPriceFeed>;
};

export async function getLiquidatePositionInstructionAsync<
  TAccountLiquidator extends string,
  TAccountUser extends string,
  TAccountUserAccount extends string,
  TAccountUserPosition extends string,
  TAccountOpenOrderAccount extends string,
  TAccountMarket extends string,
  TAccountRequestQueue extends string,
  TAccountBaseMint extends string,
  TAccountQuoteMint extends string,
  TAccountPriceFeed extends string,
  TProgramAddress extends Address = typeof PERP_PRIME_PROGRAM_ADDRESS,
>(
  input: LiquidatePositionAsyncInput<
    TAccountLiquidator,
    TAccountUser,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountOpenOrderAccount,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountBaseMint,
    TAccountQuoteMint,
    TAccountPriceFeed
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  LiquidatePositionInstruction<
    TProgramAddress,
    TAccountLiquidator,
    TAccountUser,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountOpenOrderAccount,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountBaseMint,
    TAccountQuoteMint,
    TAccountPriceFeed
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_PRIME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    liquidator: { value: input.liquidator ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    userPosition: { value: input.userPosition ?? null, isWritable: true },
    openOrderAccount: {
      value: input.openOrderAccount ?? null,
      isWritable: true,
    },
    market: { value: input.market ?? null, isWritable: false },
    requestQueue: { value: input.requestQueue ?? null, isWritable: true },
    baseMint: { value: input.baseMint ?? null, isWritable: false },
    quoteMint: { value: input.quoteMint ?? null, isWritable: false },
    priceFeed: { value: input.priceFeed ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.userAccount.value) {
    accounts.userAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            117, 115, 101, 114, 95, 97, 99, 99, 111, 117, 110, 116,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
      ],
    });
  }
  if (!accounts.market.value) {
    accounts.market.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([109, 97, 114, 107, 101, 116])),
        getAddressEncoder().encode(expectAddress(accounts.baseMint.value)),
        getAddressEncoder().encode(expectAddress(accounts.quoteMint.value)),
      ],
    });
  }
  if (!accounts.userPosition.value) {
    accounts.userPosition.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            117, 115, 101, 114, 95, 112, 111, 115, 105, 116, 105, 111, 110,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
      ],
    });
  }
  if (!accounts.openOrderAccount.value) {
    accounts.openOrderAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([111, 112, 101, 110, 95, 111, 114, 100, 101, 114, 115])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
      ],
    });
  }
  if (!accounts.requestQueue.value) {
    accounts.requestQueue.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 101, 113, 117, 101, 115, 116, 95, 113, 117, 101, 117, 101,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.liquidator),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.userPosition),
      getAccountMeta(accounts.openOrderAccount),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.requestQueue),
      getAccountMeta(accounts.baseMint),
      getAccountMeta(accounts.quoteMint),
      getAccountMeta(accounts.priceFeed),
    ],
    data: getLiquidatePositionInstructionDataEncoder().encode({}),
    programAddress,
  } as LiquidatePositionInstruction<
    TProgramAddress,
    TAccountLiquidator,
    TAccountUser,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountOpenOrderAccount,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountBaseMint,
    TAccountQuoteMint,
    TAccountPriceFeed
  >);
}

export type LiquidatePositionInput<
  TAccountLiquidator extends string = string,
  TAccountUser extends string = string,
  TAccountUserAccount extends string = string,
  TAccountUserPosition extends string = string,
  TAccountOpenOrderAccount extends string = string,
  TAccountMarket extends string = string,
  TAccountRequestQueue extends string = string,
  TAccountBaseMint extends string = string,
  TAccountQuoteMint extends string = string,
  TAccountPriceFeed extends string = string,
> = {
  liquidator: TransactionSigner<TAccountLiquidator>;
  user: Address<TAccountUser>;
  userAccount: Address<TAccountUserAccount>;
  userPosition: Address<TAccountUserPosition>;
  openOrderAccount: Address<TAccountOpenOrderAccount>;
  market: Address<TAccountMarket>;
  requestQueue: Address<TAccountRequestQueue>;
  baseMint: Address<TAccountBaseMint>;
  quoteMint: Address<TAccountQuoteMint>;
  priceFeed: Address<TAccountPriceFeed>;
};

export function getLiquidatePositionInstruction<
  TAccountLiquidator extends string,
  TAccountUser extends string,
  TAccountUserAccount extends string,
  TAccountUserPosition extends string,
  TAccountOpenOrderAccount extends string,
  TAccountMarket extends string,
  TAccountRequestQueue extends string,
  TAccountBaseMint extends string,
  TAccountQuoteMint extends string,
  TAccountPriceFeed extends string,
  TProgramAddress extends Address = typeof PERP_PRIME_PROGRAM_ADDRESS,
>(
  input: LiquidatePositionInput<
    TAccountLiquidator,
    TAccountUser,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountOpenOrderAccount,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountBaseMint,
    TAccountQuoteMint,
    TAccountPriceFeed
  >,
  config?: { programAddress?: TProgramAddress }
): LiquidatePositionInstruction<
  TProgramAddress,
  TAccountLiquidator,
  TAccountUser,
  TAccountUserAccount,
  TAccountUserPosition,
  TAccountOpenOrderAccount,
  TAccountMarket,
  TAccountRequestQueue,
  TAccountBaseMint,
  TAccountQuoteMint,
  TAccountPriceFeed
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_PRIME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    liquidator: { value: input.liquidator ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    userPosition: { value: input.userPosition ?? null, isWritable: true },
    openOrderAccount: {
      value: input.openOrderAccount ?? null,
      isWritable: true,
    },
    market: { value: input.market ?? null, isWritable: false },
    requestQueue: { value: input.requestQueue ?? null, isWritable: true },
    baseMint: { value: input.baseMint ?? null, isWritable: false },
    quoteMint: { value: input.quoteMint ?? null, isWritable: false },
    priceFeed: { value: input.priceFeed ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.liquidator),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.userPosition),
      getAccountMeta(accounts.openOrderAccount),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.requestQueue),
      getAccountMeta(accounts.baseMint),
      getAccountMeta(accounts.quoteMint),
      getAccountMeta(accounts.priceFeed),
    ],
    data: getLiquidatePositionInstructionDataEncoder().encode({}),
    programAddress,
  } as LiquidatePositionInstruction<
    TProgramAddress,
    TAccountLiquidator,
    TAccountUser,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountOpenOrderAccount,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountBaseMint,
    TAccountQuoteMint,
    TAccountPriceFeed
  >);
}

export type ParsedLiquidatePositionInstruction<
  TProgram extends string = typeof PERP_PRIME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    liquidator: TAccountMetas[0];
    user: TAccountMetas[1];
    userAccount: TAccountMetas[2];
    userPosition: TAccountMetas[3];
    openOrderAccount: TAccountMetas[4];
    market: TAccountMetas[5];
    requestQueue: TAccountMetas[6];
    baseMint: TAccountMetas[7];
    quoteMint: TAccountMetas[8];
    priceFeed: TAccountMetas[9];
  };
  data: LiquidatePositionInstructionData;
};

export function parseLiquidatePositionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedLiquidatePositionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      liquidator: getNextAccount(),
      user: getNextAccount(),
      userAccount: getNextAccount(),
      userPosition: getNextAccount(),
      openOrderAccount: getNextAccount(),
      market: getNextAccount(),
      requestQueue: getNextAccount(),
      baseMint: getNextAccount(),
      quoteMint: getNextAccount(),
      priceFeed: getNextAccount(),
    },
    data: getLiquidatePositionInstructionDataDecoder().decode(instruction.data),
  };
}
