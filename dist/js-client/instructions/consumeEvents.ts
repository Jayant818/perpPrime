/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PERP_PRIME_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CONSUME_EVENTS_DISCRIMINATOR = new Uint8Array([
  221, 145, 177, 52, 31, 47, 63, 201,
]);

export function getConsumeEventsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CONSUME_EVENTS_DISCRIMINATOR
  );
}

export type ConsumeEventsInstruction<
  TProgram extends string = typeof PERP_PRIME_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountEventQueue extends string | AccountMeta<string> = string,
  TAccountBaseMint extends string | AccountMeta<string> = string,
  TAccountQuoteMint extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? WritableSignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountEventQueue extends string
        ? WritableAccount<TAccountEventQueue>
        : TAccountEventQueue,
      TAccountBaseMint extends string
        ? ReadonlyAccount<TAccountBaseMint>
        : TAccountBaseMint,
      TAccountQuoteMint extends string
        ? ReadonlyAccount<TAccountQuoteMint>
        : TAccountQuoteMint,
      ...TRemainingAccounts,
    ]
  >;

export type ConsumeEventsInstructionData = {
  discriminator: ReadonlyUint8Array;
  maxEvents: bigint;
};

export type ConsumeEventsInstructionDataArgs = { maxEvents: number | bigint };

export function getConsumeEventsInstructionDataEncoder(): FixedSizeEncoder<ConsumeEventsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['maxEvents', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CONSUME_EVENTS_DISCRIMINATOR })
  );
}

export function getConsumeEventsInstructionDataDecoder(): FixedSizeDecoder<ConsumeEventsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['maxEvents', getU64Decoder()],
  ]);
}

export function getConsumeEventsInstructionDataCodec(): FixedSizeCodec<
  ConsumeEventsInstructionDataArgs,
  ConsumeEventsInstructionData
> {
  return combineCodec(
    getConsumeEventsInstructionDataEncoder(),
    getConsumeEventsInstructionDataDecoder()
  );
}

export type ConsumeEventsAsyncInput<
  TAccountCranker extends string = string,
  TAccountMarket extends string = string,
  TAccountEventQueue extends string = string,
  TAccountBaseMint extends string = string,
  TAccountQuoteMint extends string = string,
> = {
  cranker: TransactionSigner<TAccountCranker>;
  market?: Address<TAccountMarket>;
  eventQueue?: Address<TAccountEventQueue>;
  baseMint: Address<TAccountBaseMint>;
  quoteMint: Address<TAccountQuoteMint>;
  maxEvents: ConsumeEventsInstructionDataArgs['maxEvents'];
};

export async function getConsumeEventsInstructionAsync<
  TAccountCranker extends string,
  TAccountMarket extends string,
  TAccountEventQueue extends string,
  TAccountBaseMint extends string,
  TAccountQuoteMint extends string,
  TProgramAddress extends Address = typeof PERP_PRIME_PROGRAM_ADDRESS,
>(
  input: ConsumeEventsAsyncInput<
    TAccountCranker,
    TAccountMarket,
    TAccountEventQueue,
    TAccountBaseMint,
    TAccountQuoteMint
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ConsumeEventsInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountEventQueue,
    TAccountBaseMint,
    TAccountQuoteMint
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_PRIME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: true },
    eventQueue: { value: input.eventQueue ?? null, isWritable: true },
    baseMint: { value: input.baseMint ?? null, isWritable: false },
    quoteMint: { value: input.quoteMint ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.market.value) {
    accounts.market.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([109, 97, 114, 107, 101, 116])),
        getAddressEncoder().encode(expectAddress(accounts.baseMint.value)),
        getAddressEncoder().encode(expectAddress(accounts.quoteMint.value)),
      ],
    });
  }
  if (!accounts.eventQueue.value) {
    accounts.eventQueue.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([101, 118, 101, 110, 116, 95, 113, 117, 101, 117, 101])
        ),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.cranker),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.eventQueue),
      getAccountMeta(accounts.baseMint),
      getAccountMeta(accounts.quoteMint),
    ],
    data: getConsumeEventsInstructionDataEncoder().encode(
      args as ConsumeEventsInstructionDataArgs
    ),
    programAddress,
  } as ConsumeEventsInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountEventQueue,
    TAccountBaseMint,
    TAccountQuoteMint
  >);
}

export type ConsumeEventsInput<
  TAccountCranker extends string = string,
  TAccountMarket extends string = string,
  TAccountEventQueue extends string = string,
  TAccountBaseMint extends string = string,
  TAccountQuoteMint extends string = string,
> = {
  cranker: TransactionSigner<TAccountCranker>;
  market: Address<TAccountMarket>;
  eventQueue: Address<TAccountEventQueue>;
  baseMint: Address<TAccountBaseMint>;
  quoteMint: Address<TAccountQuoteMint>;
  maxEvents: ConsumeEventsInstructionDataArgs['maxEvents'];
};

export function getConsumeEventsInstruction<
  TAccountCranker extends string,
  TAccountMarket extends string,
  TAccountEventQueue extends string,
  TAccountBaseMint extends string,
  TAccountQuoteMint extends string,
  TProgramAddress extends Address = typeof PERP_PRIME_PROGRAM_ADDRESS,
>(
  input: ConsumeEventsInput<
    TAccountCranker,
    TAccountMarket,
    TAccountEventQueue,
    TAccountBaseMint,
    TAccountQuoteMint
  >,
  config?: { programAddress?: TProgramAddress }
): ConsumeEventsInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountMarket,
  TAccountEventQueue,
  TAccountBaseMint,
  TAccountQuoteMint
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_PRIME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: true },
    eventQueue: { value: input.eventQueue ?? null, isWritable: true },
    baseMint: { value: input.baseMint ?? null, isWritable: false },
    quoteMint: { value: input.quoteMint ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.cranker),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.eventQueue),
      getAccountMeta(accounts.baseMint),
      getAccountMeta(accounts.quoteMint),
    ],
    data: getConsumeEventsInstructionDataEncoder().encode(
      args as ConsumeEventsInstructionDataArgs
    ),
    programAddress,
  } as ConsumeEventsInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountEventQueue,
    TAccountBaseMint,
    TAccountQuoteMint
  >);
}

export type ParsedConsumeEventsInstruction<
  TProgram extends string = typeof PERP_PRIME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    cranker: TAccountMetas[0];
    market: TAccountMetas[1];
    eventQueue: TAccountMetas[2];
    baseMint: TAccountMetas[3];
    quoteMint: TAccountMetas[4];
  };
  data: ConsumeEventsInstructionData;
};

export function parseConsumeEventsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedConsumeEventsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      market: getNextAccount(),
      eventQueue: getNextAccount(),
      baseMint: getNextAccount(),
      quoteMint: getNextAccount(),
    },
    data: getConsumeEventsInstructionDataDecoder().decode(instruction.data),
  };
}
