/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PERP_PRIME_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CANCEL_ORDER_DISCRIMINATOR = new Uint8Array([
  95, 129, 237, 240, 8, 49, 223, 132,
]);

export function getCancelOrderDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_ORDER_DISCRIMINATOR
  );
}

export type CancelOrderInstruction<
  TProgram extends string = typeof PERP_PRIME_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountRequestQueue extends string | AccountMeta<string> = string,
  TAccountUserGlobalAccount extends string | AccountMeta<string> = string,
  TAccountOpenOrdersAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      TAccountRequestQueue extends string
        ? WritableAccount<TAccountRequestQueue>
        : TAccountRequestQueue,
      TAccountUserGlobalAccount extends string
        ? ReadonlyAccount<TAccountUserGlobalAccount>
        : TAccountUserGlobalAccount,
      TAccountOpenOrdersAccount extends string
        ? ReadonlyAccount<TAccountOpenOrdersAccount>
        : TAccountOpenOrdersAccount,
      ...TRemainingAccounts,
    ]
  >;

export type CancelOrderInstructionData = {
  discriminator: ReadonlyUint8Array;
  clientOrderId: bigint;
};

export type CancelOrderInstructionDataArgs = { clientOrderId: number | bigint };

export function getCancelOrderInstructionDataEncoder(): FixedSizeEncoder<CancelOrderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['clientOrderId', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CANCEL_ORDER_DISCRIMINATOR })
  );
}

export function getCancelOrderInstructionDataDecoder(): FixedSizeDecoder<CancelOrderInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['clientOrderId', getU64Decoder()],
  ]);
}

export function getCancelOrderInstructionDataCodec(): FixedSizeCodec<
  CancelOrderInstructionDataArgs,
  CancelOrderInstructionData
> {
  return combineCodec(
    getCancelOrderInstructionDataEncoder(),
    getCancelOrderInstructionDataDecoder()
  );
}

export type CancelOrderAsyncInput<
  TAccountUser extends string = string,
  TAccountMarket extends string = string,
  TAccountRequestQueue extends string = string,
  TAccountUserGlobalAccount extends string = string,
  TAccountOpenOrdersAccount extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  market: Address<TAccountMarket>;
  requestQueue?: Address<TAccountRequestQueue>;
  userGlobalAccount?: Address<TAccountUserGlobalAccount>;
  openOrdersAccount?: Address<TAccountOpenOrdersAccount>;
  clientOrderId: CancelOrderInstructionDataArgs['clientOrderId'];
};

export async function getCancelOrderInstructionAsync<
  TAccountUser extends string,
  TAccountMarket extends string,
  TAccountRequestQueue extends string,
  TAccountUserGlobalAccount extends string,
  TAccountOpenOrdersAccount extends string,
  TProgramAddress extends Address = typeof PERP_PRIME_PROGRAM_ADDRESS,
>(
  input: CancelOrderAsyncInput<
    TAccountUser,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountUserGlobalAccount,
    TAccountOpenOrdersAccount
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CancelOrderInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountUserGlobalAccount,
    TAccountOpenOrdersAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_PRIME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: false },
    requestQueue: { value: input.requestQueue ?? null, isWritable: true },
    userGlobalAccount: {
      value: input.userGlobalAccount ?? null,
      isWritable: false,
    },
    openOrdersAccount: {
      value: input.openOrdersAccount ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.requestQueue.value) {
    accounts.requestQueue.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 101, 113, 117, 101, 115, 116, 95, 113, 117, 101, 117, 101,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
      ],
    });
  }
  if (!accounts.userGlobalAccount.value) {
    accounts.userGlobalAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            117, 115, 101, 114, 95, 97, 99, 99, 111, 117, 110, 116,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
      ],
    });
  }
  if (!accounts.openOrdersAccount.value) {
    accounts.openOrdersAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([111, 112, 101, 110, 95, 111, 114, 100, 101, 114, 115])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.requestQueue),
      getAccountMeta(accounts.userGlobalAccount),
      getAccountMeta(accounts.openOrdersAccount),
    ],
    data: getCancelOrderInstructionDataEncoder().encode(
      args as CancelOrderInstructionDataArgs
    ),
    programAddress,
  } as CancelOrderInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountUserGlobalAccount,
    TAccountOpenOrdersAccount
  >);
}

export type CancelOrderInput<
  TAccountUser extends string = string,
  TAccountMarket extends string = string,
  TAccountRequestQueue extends string = string,
  TAccountUserGlobalAccount extends string = string,
  TAccountOpenOrdersAccount extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  market: Address<TAccountMarket>;
  requestQueue: Address<TAccountRequestQueue>;
  userGlobalAccount: Address<TAccountUserGlobalAccount>;
  openOrdersAccount: Address<TAccountOpenOrdersAccount>;
  clientOrderId: CancelOrderInstructionDataArgs['clientOrderId'];
};

export function getCancelOrderInstruction<
  TAccountUser extends string,
  TAccountMarket extends string,
  TAccountRequestQueue extends string,
  TAccountUserGlobalAccount extends string,
  TAccountOpenOrdersAccount extends string,
  TProgramAddress extends Address = typeof PERP_PRIME_PROGRAM_ADDRESS,
>(
  input: CancelOrderInput<
    TAccountUser,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountUserGlobalAccount,
    TAccountOpenOrdersAccount
  >,
  config?: { programAddress?: TProgramAddress }
): CancelOrderInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountMarket,
  TAccountRequestQueue,
  TAccountUserGlobalAccount,
  TAccountOpenOrdersAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_PRIME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: false },
    requestQueue: { value: input.requestQueue ?? null, isWritable: true },
    userGlobalAccount: {
      value: input.userGlobalAccount ?? null,
      isWritable: false,
    },
    openOrdersAccount: {
      value: input.openOrdersAccount ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.requestQueue),
      getAccountMeta(accounts.userGlobalAccount),
      getAccountMeta(accounts.openOrdersAccount),
    ],
    data: getCancelOrderInstructionDataEncoder().encode(
      args as CancelOrderInstructionDataArgs
    ),
    programAddress,
  } as CancelOrderInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountMarket,
    TAccountRequestQueue,
    TAccountUserGlobalAccount,
    TAccountOpenOrdersAccount
  >);
}

export type ParsedCancelOrderInstruction<
  TProgram extends string = typeof PERP_PRIME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    market: TAccountMetas[1];
    requestQueue: TAccountMetas[2];
    userGlobalAccount: TAccountMetas[3];
    openOrdersAccount: TAccountMetas[4];
  };
  data: CancelOrderInstructionData;
};

export function parseCancelOrderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCancelOrderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      market: getNextAccount(),
      requestQueue: getNextAccount(),
      userGlobalAccount: getNextAccount(),
      openOrdersAccount: getNextAccount(),
    },
    data: getCancelOrderInstructionDataDecoder().decode(instruction.data),
  };
}
