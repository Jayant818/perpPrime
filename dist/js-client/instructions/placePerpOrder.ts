/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PERP_PRIME_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';
import {
  getOrderPositionDecoder,
  getOrderPositionEncoder,
  getOrderSideDecoder,
  getOrderSideEncoder,
  getOrderTypeDecoder,
  getOrderTypeEncoder,
  type OrderPosition,
  type OrderPositionArgs,
  type OrderSide,
  type OrderSideArgs,
  type OrderType,
  type OrderTypeArgs,
} from '../types';

export const PLACE_PERP_ORDER_DISCRIMINATOR = new Uint8Array([
  69, 161, 93, 202, 120, 126, 76, 185,
]);

export function getPlacePerpOrderDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PLACE_PERP_ORDER_DISCRIMINATOR
  );
}

export type PlacePerpOrderInstruction<
  TProgram extends string = typeof PERP_PRIME_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountUserPosition extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountOpenOrdersAccount extends string | AccountMeta<string> = string,
  TAccountRequestQueue extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountUserPosition extends string
        ? ReadonlyAccount<TAccountUserPosition>
        : TAccountUserPosition,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountOpenOrdersAccount extends string
        ? WritableAccount<TAccountOpenOrdersAccount>
        : TAccountOpenOrdersAccount,
      TAccountRequestQueue extends string
        ? WritableAccount<TAccountRequestQueue>
        : TAccountRequestQueue,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PlacePerpOrderInstructionData = {
  discriminator: ReadonlyUint8Array;
  pair: string;
  amountInUi: bigint;
  side: OrderSide;
  qtyInUi: bigint;
  position: OrderPosition;
  margin: bigint;
  orderType: OrderType;
  clientOrderId: bigint;
};

export type PlacePerpOrderInstructionDataArgs = {
  pair: string;
  amountInUi: number | bigint;
  side: OrderSideArgs;
  qtyInUi: number | bigint;
  position: OrderPositionArgs;
  margin: number | bigint;
  orderType: OrderTypeArgs;
  clientOrderId: number | bigint;
};

export function getPlacePerpOrderInstructionDataEncoder(): Encoder<PlacePerpOrderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['pair', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['amountInUi', getU64Encoder()],
      ['side', getOrderSideEncoder()],
      ['qtyInUi', getU64Encoder()],
      ['position', getOrderPositionEncoder()],
      ['margin', getU64Encoder()],
      ['orderType', getOrderTypeEncoder()],
      ['clientOrderId', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PLACE_PERP_ORDER_DISCRIMINATOR })
  );
}

export function getPlacePerpOrderInstructionDataDecoder(): Decoder<PlacePerpOrderInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['pair', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['amountInUi', getU64Decoder()],
    ['side', getOrderSideDecoder()],
    ['qtyInUi', getU64Decoder()],
    ['position', getOrderPositionDecoder()],
    ['margin', getU64Decoder()],
    ['orderType', getOrderTypeDecoder()],
    ['clientOrderId', getU64Decoder()],
  ]);
}

export function getPlacePerpOrderInstructionDataCodec(): Codec<
  PlacePerpOrderInstructionDataArgs,
  PlacePerpOrderInstructionData
> {
  return combineCodec(
    getPlacePerpOrderInstructionDataEncoder(),
    getPlacePerpOrderInstructionDataDecoder()
  );
}

export type PlacePerpOrderAsyncInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
  TAccountUserAccount extends string = string,
  TAccountUserPosition extends string = string,
  TAccountMarket extends string = string,
  TAccountOpenOrdersAccount extends string = string,
  TAccountRequestQueue extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config?: Address<TAccountConfig>;
  userAccount?: Address<TAccountUserAccount>;
  userPosition?: Address<TAccountUserPosition>;
  market?: Address<TAccountMarket>;
  openOrdersAccount?: Address<TAccountOpenOrdersAccount>;
  requestQueue?: Address<TAccountRequestQueue>;
  systemProgram?: Address<TAccountSystemProgram>;
  pair: PlacePerpOrderInstructionDataArgs['pair'];
  amountInUi: PlacePerpOrderInstructionDataArgs['amountInUi'];
  side: PlacePerpOrderInstructionDataArgs['side'];
  qtyInUi: PlacePerpOrderInstructionDataArgs['qtyInUi'];
  position: PlacePerpOrderInstructionDataArgs['position'];
  margin: PlacePerpOrderInstructionDataArgs['margin'];
  orderType: PlacePerpOrderInstructionDataArgs['orderType'];
  clientOrderId: PlacePerpOrderInstructionDataArgs['clientOrderId'];
};

export async function getPlacePerpOrderInstructionAsync<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TAccountUserAccount extends string,
  TAccountUserPosition extends string,
  TAccountMarket extends string,
  TAccountOpenOrdersAccount extends string,
  TAccountRequestQueue extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERP_PRIME_PROGRAM_ADDRESS,
>(
  input: PlacePerpOrderAsyncInput<
    TAccountOwner,
    TAccountConfig,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountMarket,
    TAccountOpenOrdersAccount,
    TAccountRequestQueue,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  PlacePerpOrderInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountMarket,
    TAccountOpenOrdersAccount,
    TAccountRequestQueue,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_PRIME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    userPosition: { value: input.userPosition ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    openOrdersAccount: {
      value: input.openOrdersAccount ?? null,
      isWritable: true,
    },
    requestQueue: { value: input.requestQueue ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.userAccount.value) {
    accounts.userAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            117, 115, 101, 114, 95, 97, 99, 99, 111, 117, 110, 116,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }
  if (!accounts.market.value) {
    accounts.market.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([109, 97, 114, 107, 101, 116])),
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()).encode(
          expectSome(args.pair)
        ),
      ],
    });
  }
  if (!accounts.userPosition.value) {
    accounts.userPosition.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            117, 115, 101, 114, 95, 112, 111, 115, 105, 116, 105, 111, 110,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
      ],
    });
  }
  if (!accounts.openOrdersAccount.value) {
    accounts.openOrdersAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([111, 112, 101, 110, 95, 111, 114, 100, 101, 114, 115])
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
      ],
    });
  }
  if (!accounts.requestQueue.value) {
    accounts.requestQueue.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 101, 113, 117, 101, 115, 116, 95, 113, 117, 101, 117, 101,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.userPosition),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.openOrdersAccount),
      getAccountMeta(accounts.requestQueue),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPlacePerpOrderInstructionDataEncoder().encode(
      args as PlacePerpOrderInstructionDataArgs
    ),
    programAddress,
  } as PlacePerpOrderInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountMarket,
    TAccountOpenOrdersAccount,
    TAccountRequestQueue,
    TAccountSystemProgram
  >);
}

export type PlacePerpOrderInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
  TAccountUserAccount extends string = string,
  TAccountUserPosition extends string = string,
  TAccountMarket extends string = string,
  TAccountOpenOrdersAccount extends string = string,
  TAccountRequestQueue extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config: Address<TAccountConfig>;
  userAccount: Address<TAccountUserAccount>;
  userPosition: Address<TAccountUserPosition>;
  market: Address<TAccountMarket>;
  openOrdersAccount: Address<TAccountOpenOrdersAccount>;
  requestQueue: Address<TAccountRequestQueue>;
  systemProgram?: Address<TAccountSystemProgram>;
  pair: PlacePerpOrderInstructionDataArgs['pair'];
  amountInUi: PlacePerpOrderInstructionDataArgs['amountInUi'];
  side: PlacePerpOrderInstructionDataArgs['side'];
  qtyInUi: PlacePerpOrderInstructionDataArgs['qtyInUi'];
  position: PlacePerpOrderInstructionDataArgs['position'];
  margin: PlacePerpOrderInstructionDataArgs['margin'];
  orderType: PlacePerpOrderInstructionDataArgs['orderType'];
  clientOrderId: PlacePerpOrderInstructionDataArgs['clientOrderId'];
};

export function getPlacePerpOrderInstruction<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TAccountUserAccount extends string,
  TAccountUserPosition extends string,
  TAccountMarket extends string,
  TAccountOpenOrdersAccount extends string,
  TAccountRequestQueue extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERP_PRIME_PROGRAM_ADDRESS,
>(
  input: PlacePerpOrderInput<
    TAccountOwner,
    TAccountConfig,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountMarket,
    TAccountOpenOrdersAccount,
    TAccountRequestQueue,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): PlacePerpOrderInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountConfig,
  TAccountUserAccount,
  TAccountUserPosition,
  TAccountMarket,
  TAccountOpenOrdersAccount,
  TAccountRequestQueue,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_PRIME_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    userPosition: { value: input.userPosition ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    openOrdersAccount: {
      value: input.openOrdersAccount ?? null,
      isWritable: true,
    },
    requestQueue: { value: input.requestQueue ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.userPosition),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.openOrdersAccount),
      getAccountMeta(accounts.requestQueue),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPlacePerpOrderInstructionDataEncoder().encode(
      args as PlacePerpOrderInstructionDataArgs
    ),
    programAddress,
  } as PlacePerpOrderInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountUserAccount,
    TAccountUserPosition,
    TAccountMarket,
    TAccountOpenOrdersAccount,
    TAccountRequestQueue,
    TAccountSystemProgram
  >);
}

export type ParsedPlacePerpOrderInstruction<
  TProgram extends string = typeof PERP_PRIME_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    config: TAccountMetas[1];
    userAccount: TAccountMetas[2];
    userPosition: TAccountMetas[3];
    market: TAccountMetas[4];
    openOrdersAccount: TAccountMetas[5];
    requestQueue: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: PlacePerpOrderInstructionData;
};

export function parsePlacePerpOrderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedPlacePerpOrderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      config: getNextAccount(),
      userAccount: getNextAccount(),
      userPosition: getNextAccount(),
      market: getNextAccount(),
      openOrdersAccount: getNextAccount(),
      requestQueue: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPlacePerpOrderInstructionDataDecoder().decode(instruction.data),
  };
}
